import { Poppins, Lusitana } from 'next/font/google';

export const fontInter = Poppins({
  subsets: ['latin'],
  weight: '500',
});

export const fontLusitana = Lusitana({
  subsets: ['latin'],
  weight: '400',
});
